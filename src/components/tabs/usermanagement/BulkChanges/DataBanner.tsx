import React from "react";

// Future implementation just advising the users when the data was gathered
const DataBanner = ()  => {
  return (
    <div>
      Banner
    </div>
  );
};

export default DataBanner;